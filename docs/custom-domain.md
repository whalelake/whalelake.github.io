# Custom Domain Setup

When the domain is ready, add a file named `CNAME` at the repository root.

For an apex domain:

```text
example.com
```

For a subdomain:

```text
blog.example.com
```

Then open GitHub repository settings and configure **Pages > Custom domain**. Verify the domain in GitHub before connecting it, then enable HTTPS after DNS propagation completes.
